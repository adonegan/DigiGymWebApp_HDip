@model IEnumerable<DateTime>

@{
    ViewBag.Title = "Food Diary";
}

<div class="container">
    <div class="row">
        <h2>Food Diary</h2>

        <p>
            <a asp-action="Create">Create New</a>
        </p>

        <ul>
            @foreach (var date in Model)
            {
                <li>
                    <a href="@Url.Action("Dates", new { date = date.ToString("yyyy-MM-dd") })">
                        @date.ToString("d")
                    </a>
                </li>
            }
        </ul>
    </div>
</div>